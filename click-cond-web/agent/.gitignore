# Configuração local (segredos: API_URL, tokens)
.env

# Artefatos do build do executável
*.exe
*.exe.sha256
*.blob
click-agent

# Artefato do bundle (esbuild) — gerado a partir de src/, não versionar
dist/

node_modules/

# estado de runtime do agente
device-baselines.json
events-queue.jsonl
agent-stdout.log
agent-stderr.log

# Harness: cópia descartável do agente usada nos testes
harness/.tmp-agent/
harness/saida.txt
