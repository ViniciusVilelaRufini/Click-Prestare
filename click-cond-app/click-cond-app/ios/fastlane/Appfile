# Identificadores públicos do app — não são segredo, e por isso ficam
# versionados. O que é segredo (chave .p8, senha do match) vem por variável de
# ambiente, dos Secrets do GitHub.
app_identifier("br.com.clickprestare.app")
team_id("8B2F8N375Z")
